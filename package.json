{
  "name": "propanes-monorepo",
  "private": true,
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:e2e": "pnpm --filter @propanes/e2e test",
    "test:e2e:update": "pnpm --filter @propanes/e2e test:update",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
