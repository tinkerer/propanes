{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "propanes-agent.serviceAccountName" . }}
  labels:
    {{- include "propanes-agent.labels" . | nindent 4 }}
{{- end }}
